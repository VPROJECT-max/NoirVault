# NoirVault Roadmap

## Future ideas

- Optional authenticator-app second factor: require a TOTP code from an app such as Google Authenticator during vault unlock.
- Optional encrypted cloud storage: sync only encrypted vault envelopes; the cloud service must never receive a master password or plaintext vault data.
- Cross-device recovery and enrollment that never exports an unencrypted vault or passkey private key.
